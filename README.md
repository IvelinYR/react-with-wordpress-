## Installation

1. Clone this repo in `git clone https://github.com/`

2. `git checkout branchname`

3. Run `npm install`

## Commands

- `start` Runs webpack dev server for development ( in watch mode )

Common
- `prod` Runs webpack in production mode



# wordpress-task
